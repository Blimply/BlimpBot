@page "/"
@using BlimpBot.Pages
@model HomePageModel

@{
    ViewData["Title"] = "BlimpBot HomePage";
}

<h1>Hello and welcome to BlimpBot's homepage! Hope you have a nice day.</h1>

I am currently in @(Model.Chats.Count) chats. The names of these chats are:
<table>
    <tr>
        <th>Chat Name</th>
        <th>Last message received (AWST)</th>
    </tr>
    @foreach (var chat in Model.Chats)
    {
        <tr>
            <td>
                @Html.DisplayFor(i=>chat.Name)
            </td>
            <td>
                @TimeZoneInfo.ConvertTimeFromUtc(chat.LastMessageReceived
                                                               .Value
                                                               .ToUniversalTime(),
                                                           TimeZoneInfo.FindSystemTimeZoneById("W. Australia Standard Time")).ToString("yyyy-MMM-dd hh:mm:ss tt")
            </td>
        </tr>
    }
</table>


